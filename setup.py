#!/usr/bin/env python

from distutils.core import setup

setup(name='ls_report_agent',
      version='0.2',
      description='Agent for collecting a report from the LegalServer report API',
      author='Nate Vogel',
      packages=['ls_report_agent'])
